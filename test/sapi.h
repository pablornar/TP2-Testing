#include "stdint.h" 

void uartConfig(int uart, int velocidad);
void uartConfig(int uart, int velocidad);
void adcConfig(int valor);
int gpioRead(int valor);
char uartReadByte(int uart, int valor);
void uartWriteString(int uart, char caracter);
void uartWriteByte(int uart, int dato);
